#pragma once
#include <QGraphicsObject>

/**
 * @brief The LogicalScenarioView class
 *
 * @todo In order to do this we also have to make a presenter, since it is what positions the elements.
 */

class LogicalScenarioView : public QGraphicsObject
{
	Q_OBJECT

	public:

		virtual ~LogicalScenarioView() = default;

	private:

};

