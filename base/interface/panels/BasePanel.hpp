#pragma once
/*#include <interface/panels/Panel.hpp>

namespace iscore
{
	// The central widget
	class BasePanel : protected Panel
	{
		public:
			using Panel::makeView;
			using Panel::makeModel;
			virtual PanelPresenter* makePresenter(DocumentPresenter* parent_presenter, PanelModel* model, PanelView* view) = 0;
			
		private:
			virtual PanelPresenter* makePresenter(Presenter* parent_presenter, PanelModel* model, PanelView* view){}

	};
}
*/