#pragma once

/**
 * \namespace Inspector
 * \brief Classes used for making and extending the inspector (default right
 * panel).
 *
 * It is split in two :
 * * The library score_lib_inspector contains the interface that is used
 *   in other plugins to provide the inspector for each element.
 * * The plug-in score_plugin_inspector contains the actual inspector panel
 * definition.
 *
 * \see Inspector::InspectorWidgetBase
 */
